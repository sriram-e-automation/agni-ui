## What changed

<!-- Name the components. One line each. -->

## Review gate (CONTRIBUTING.md §4)

- [ ] `.d.ts` matches `.tsx` — the published contract is the one people read
- [ ] The specimen card **shows** the new variant / state (not just "the code supports it")
- [ ] `@version` bumped on every changed component, at the right level
- [ ] A changeset is included, and it says what a consumer must do
- [ ] `readme.md` names any new component
- [ ] If class strings changed: file added to `MIGRATED[]`, `agniui.css` rebuilt

## Migration required?

<!-- Delete if none. Otherwise write the exact line a consumer needs. -->
